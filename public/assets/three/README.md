# Three.js assets

Place `.glb`, `.gltf`, textures, and other static scene assets here. Files in
`public/` are served from the site root, for example:

```js
loader.load("/assets/three/model.glb");
```
